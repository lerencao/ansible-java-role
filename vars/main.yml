---
# helper variables
oracle_jdk_mirror_base: "http://download.oracle.com/otn-pub/java/jdk"

oracle_jdk_pkg_name: "jdk-{{ oracle.minor_version }}u{{ oracle.update_version }}-linux-x64.tar.gz"
oracle_jdk_dir_name: "jdk{{ oracle.major_version }}.{{ oracle.minor_version }}.{{ oracle.patch_version }}_{{ oracle.update_version }}"
oracle_jdk_pkg_mirror: "{{ oracle_jdk_mirror_base }}/{{ oracle.minor_version }}u{{ oracle.update_version }}-b{{ oracle.build_version }}/{{ oracle_jdk_pkg_name }}"
